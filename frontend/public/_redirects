# Redirect `/api/*` to the deployed backend server (e.g. Render, VPS, or Fly.io)
# Replace `https://your-backend-api-url.com` with your actual backend URL after deployment.
/api/* https://your-backend-api-url.com/:splat 200
